---
description: GitHub board work is 8081 only — never touch or cite 8080 unless she names it
alwaysApply: true
---

# GitHub board = 8081 only

Hard rule for diana’s **GitHub** dashboard work (this repo / `dianacodesit/my-dashboard` checkout, and `/Users/diana/github-my-dashboard`).

She stood up **8081** so GitHub work stays off the live **8080** board. Do not blur them.

## Ports / trees

| | Port | Tree |
|---|---|---|
| **GitHub board (default for this repo)** | `http://127.0.0.1:8081/` | `/Users/diana/github-my-dashboard` (LaunchAgent serve; sync from this checkout) |
| Live board | `http://127.0.0.1:8080/` | `/Users/diana/my-dashboard` |

## Always (GitHub / this checkout)

1. Edit in the **GitHub workspace** (`…/Documents/GitHub/dianacodesit/my-dashboard` or this repo root).
2. Sync product HTML / assets to **`/Users/diana/github-my-dashboard`** so **8081** shows the change.
3. Verify / hard-refresh / give URLs on **8081 only**.
4. Kickstart / bounce only the **8081** LaunchAgent (`com.diana.my-dashboard-github-8081`) when needed.

## Never (unless she explicitly names 8080 / live)

1. Sync or copy into `/Users/diana/my-dashboard`
2. Tell her to open or hard-refresh **8080**
3. Cite “8080 or 8081” as if either is fine
4. Touch the live 8080 LaunchAgent / serve for GitHub-board tasks
5. Treat `everything-page-only`’s live path as permission to write the live tree during GitHub-board work — **8081 tree wins** for product sync

## Override

Only Diana saying **8080**, **live board**, or **`/Users/diana/my-dashboard`** by name unlocks that tree for that ask.
