---
description: Never invent air collage or strip layout/badges/resize/reframe
alwaysApply: true
---

# No “air collage” product — do not strip chrome

Hard rule for diana’s dashboard (`/Users/diana/my-dashboard`).

## Banned

- Calling anything **“vision collage with air”** / **air collage** as a product Diana asked for
- Using Sep 18+ / `vision-air-collage` as an excuse to strip UI she already had

## What Sep 18+ actually is

**Vision collage** — same magazine pack as Aug 26, **12px gutters**, no dash-mosaic rooms. That is all. Not a new mode that deletes chrome.

## Never strip on Sep 18+ (or any day)

1. **layout** button (before **carry** / **+**)
2. Collapsed-day **open-task number badges** (`.day-count`, right of date)
3. **Tile resize** handles (`.vision-tile-resize`) — ability stays; glyph stays invisible per `no-resize-handle-symbol`
4. **Photo reframe** (`.vision-photo-reframe`)
5. Any other chrome that already worked on Sep 16–17 / mosaic days

## Forbidden patterns

- `display: none` on `.vision-layout-control` for `vision-air-collage`
- `display: none` on `.vision-tile-resize` / `.vision-photo-reframe` for `vision-air-collage`
- JS that `remove()`s resize/reframe/layout in the Sep 18+ pack path
- Deleting `layoutConfig` from mosaic store just to kill the layout button
- Blanket hiding `.day-count` on all overview-era / Sep dates

## If any of the above is missing

Restore immediately. Do not ask. Do not invent another packing brand name.
