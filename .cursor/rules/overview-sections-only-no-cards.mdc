---
description: Legacy stub — depth / sections-only rules live in overview-no-cards (four layers, overview-reclick popup)
alwaysApply: true
---

# Overview sections-only — see `overview-no-cards`

This file is a **pointer only**. Do not maintain a second, conflicting overview/details-only rule here.

**Canonical rule:** `overview-no-cards.mdc`

- Four layers: **sections · subsections · cards · tasks**
- Depth: re-click **overview** → vertical popup to the **left** of that pill (not a second row under the pills)
- Sections depth = clean collage (no cards/polaroids on the unzoomed board)

If cards reappear in sections mode, fix per `overview-no-cards` — both `everything.html` and `prototype.html`.
